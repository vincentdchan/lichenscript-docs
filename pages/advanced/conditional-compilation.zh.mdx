---
title: 条件编译
---

# 条件编译

有时候你需要一些代码只在某个平台下进行编译，这个时候你就需要条件编译。

你可以使用 `platform` 装饰器来告诉编译器你想把接下来的代码用在那些平台当中。

比如说，你只想在某些平台下使用 `fork` 函数，你就可以这样写：

```js
@platform("native")
function fork() {
	// TODO
}
```

你也可以添加多个平台：

```js
@platform("native", "wasm32")
function fork() {
	// TODO
}
```
